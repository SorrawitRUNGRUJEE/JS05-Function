let matchPoint = (matchWin,matchDraw) => {
      totalPoint = 0
      matchWin *= 3
      matchDraw *= 1
      totalPoint = matchWin + matchDraw

      return `match point is ${totalPoint}`
      
      

//    if(result == "win")console.log(`you got 3 point`)
//    if(result == "draw")console.log(`you got 1 point`)

}